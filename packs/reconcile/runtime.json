{
  "version": 1,
  "command": "bin/reconcile-runtime",
  "timeout_seconds": 120,
  "max_continuations": 0,
  "tools": [
    { "name": "import_statement", "description": "Import a bank statement CSV (relative path in the folder). Columns are detected: date, description/memo/payee, and either amount or debit+credit. Replaces the previous statement and runs a first match.", "risk": "write",
      "input_schema": { "type": "object", "properties": { "file": { "type": "string" } }, "required": ["file"], "additionalProperties": false } },
    { "name": "reconcile", "description": "Match statement lines to ledger receipts and paid invoices by exact amount within a date window (default 3 days), then description similarity. Shows what is unexplained on either side.", "risk": "write",
      "input_schema": { "type": "object", "properties": { "window_days": { "type": "integer", "minimum": 0, "maximum": 30 } }, "additionalProperties": false } },
    { "name": "explain_line", "description": "Manually match a statement line to a book entry (ref like receipt#12 or invoice:INV-0003) or record a note explaining it (bank fee, transfer, personal).", "risk": "write",
      "input_schema": { "type": "object", "properties": { "line": { "type": "string" }, "ref": { "type": "string" }, "note": { "type": "string" } }, "required": ["line"], "additionalProperties": false } }
  ]
}
