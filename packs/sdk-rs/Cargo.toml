[package]
name = "dextui_pack_sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared Rust core for DextUI consumer packs: Pack Runtime Protocol v1 framing, money, CSV, dates, safe paths, view fences."

[lib]
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
